# Server Configuration
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# Database Configuration
# Replace <password> with actual password in your local .env only
DATABASE_URL=postgresql://neondb_owner:<password>@<your-neon-host>/<database>?sslmode=require

# JWT Secret (required in production, e.g. `openssl rand -base64 32`)
JWT_TOKEN=<your-jwt-secret>

# Arcjet
ARCJET_KEY=ajkey_<example>

# Swagger / API base URL (optional, defaults to http://localhost:3000)
# API_BASE_URL=https://<your-alb-dns>.elb.amazonaws.com
